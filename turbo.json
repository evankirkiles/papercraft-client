{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "lint": {},
    "typecheck": {},
    "dev": {
      "dependsOn": ["@paperarium/client#build:dev"],
      "with": ["pp_server#dev"],
      "persistent": true,
      "cache": false
    },
    "build:dev": {
      "outputs": ["dist/**"],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "preview": {
      "dependsOn": ["build"],
      "persistent": true,
      "cache": false
    }
  }
}
